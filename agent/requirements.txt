# Agent 服务依赖（Python 3.11+）
# 说明：MVP 采用 FastAPI + psycopg2 + pgvector 直连实现，未强制依赖 Google ADK / A2A
# （多智能体编排在单进程内以模块形式落地，待团队选型后平滑替换）。

# Web 服务
fastapi>=0.111.0
uvicorn>=0.30.0

# 数据 / 向量检索（material_chunks 解析写 + 检索只读）
psycopg2-binary>=2.9.9
pgvector>=0.2.5

# 本地确定性嵌入（离线，无模型下载）
numpy>=1.26.0

# 配置 / 校验
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# 可选：OpenAI 兼容嵌入 / 生成（设置 EMBEDDING_PROVIDER=openai 且 LLM_API_KEY 非空时启用）
httpx>=0.27.0

# 测试 / 质量门禁
pytest>=8.0.0
ruff>=0.5.0
