# ruff 配置（pre-commit 门禁：ruff format + ruff check）
line-length = 100
target-version = "py311"

[format]
quote-style = "double"

[lint]
select = ["E", "F", "I", "B", "UP", "W"]
ignore = ["E501"]

[lint.isort]
known-first-party = ["agent"]
